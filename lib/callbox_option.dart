import 'package:flutter/material.dart';

class OptionCallBox extends StatelessWidget {
  const OptionCallBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
